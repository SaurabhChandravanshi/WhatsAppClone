const initialState = [{name:'Saurabh', phone:'8458835494'}];
const usersReducer = (state = initialState, action) => {
    
    switch(action.type) {
        default:
            return state;
    }
}

export default usersReducer;